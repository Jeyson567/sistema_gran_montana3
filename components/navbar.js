export const createNavbar = (title = "Restaurant System") => `
  <header class="flex items-center justify-between rounded-2xl border border-zinc-800 bg-zinc-900 p-4">
    <h1 class="text-xl font-bold">${title}</h1>
  </header>
`;
